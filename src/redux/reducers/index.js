import {combineReducers} from 'redux'
import postsBySubreddit from './postSubredditReducer'
import selectSubreddit from './selectedSubredditReducer'

export default rootReducer = combineReducers({
    postsBySubreddit,
    selectSubreddit
})

export default rootReducer
